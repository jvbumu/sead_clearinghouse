<?php

namespace Services {

    class EncryptPasswordService extends ServiceBase {

        // private static $encryption_string = null;
        
        // // public function encode($value) { 
        // //     //throw new \Exception($this->locator->getEncryptService()->encode(EncryptPasswordService::getEncryptionKey(), "secret"));
        // //     return $this->locator->getEncryptService()->encode(EncryptPasswordService::getEncryptionKey(), $value);
        // // }

        // public function decode($value) {
        //     //throw new \Exception($this->locator->getEncryptService()->encode(EncryptPasswordService::getEncryptionKey(), "secret"));
        //     return $this->locator->getEncryptService()->decode(EncryptPasswordService::getEncryptionKey(), $value);
        // }
        
        // private static function getEncryptionKey(){
        //     if (strtoupper(substr(PHP_OS, 0, 3)) === 'WIN') {
        //         return require_once $_SERVER['DOCUMENT_ROOT'] . '\conf\clearing_house_login_key.php';
        //     }
        //     return self::$encryption_string ?: (require_once '/www/conf/clearing_house_login_key.php');
        // }
    }
    
}